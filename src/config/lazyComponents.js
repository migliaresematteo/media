import { lazy } from 'react';

export const VideoSection = lazy(() => import('../components/Video/VideoSection'));
export const HeaderComponent = lazy(() => import('../components/Header/HeaderComponent'));
